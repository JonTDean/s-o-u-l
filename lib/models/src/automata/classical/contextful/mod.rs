//! Type‑1 **Context‑Sensitive** automata (linear‑bounded automata).

use bevy::prelude::*;

pub struct ContextSensitiveAutomataPlugin;

impl Plugin for ContextSensitiveAutomataPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: add LBA rules
    }
}

/* ───── sub‑modules (stubs) ───── */
pub mod lba_examples;
