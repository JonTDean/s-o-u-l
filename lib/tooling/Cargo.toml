[package]
name = "tooling"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["bevy_render", "bevy_pbr"] }
bitflags    = { workspace = true }

engine-core = { path = "../engine/engine-core" }